<Window x:Class="EasySaveGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EasySaveGUI.ViewModels"
        mc:Ignorable="d"
        Title="EasySave" Height="450" Width="800"
        Icon="..\assets\EasySave_logo.png">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Window.Resources>
        <ResourceDictionary Source="../Ressources/NavbarButtonStyle.xaml"/>
    </Window.Resources>

    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top">
                <Grid Margin="0,5,0,5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="28"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="125"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="62.5"/>
                        <ColumnDefinition Width="62.5"/>
                        <ColumnDefinition Width="5"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Source="..\assets\EasySave_logo.png" Margin="0,0,10,0" Width="30" Height="30"/>
                        <TextBlock Text="EasySave" FontSize="15" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Home" Style="{DynamicResource ButtonStyle}" Command="{Binding NavigateHomeCommand}" Margin="5,0,5,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Settings" Style="{DynamicResource ButtonStyle}" Command="{Binding NavigateSettingsCommand}" Margin="5,0,5,0"/>
                    </StackPanel>

                </Grid>
                <Separator Height="2" HorizontalAlignment="Stretch" Background="LightGray"/>
            </StackPanel>
            <ContentControl Content="{Binding CurrentView}" DockPanel.Dock="Bottom" Margin="10"/>
        </DockPanel>
    </Grid>
</Window>
