<UserControl x:Class="EasySaveGUI.Views.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:EasySaveGUI.Views"
      xmlns:viewModels="clr-namespace:EasySaveGUI.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModels:SettingsViewModel />
    </UserControl.DataContext>

    <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="30" FontWeight="Bold" Text="{Binding WelcomeMessage}" />

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,100,0,0" Text="Language: " />
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,100,0,0" Text="{Binding Language, Mode=TwoWay}" Width="200"/>

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,150,0,0" Text="Log Path: " />
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,150,0,0" Text="{Binding LogPath, Mode=TwoWay}" Width="200"/>

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,200,0,0" Text="State Path: " />
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,200,0,0" Text="{Binding StatePath, Mode=TwoWay}" Width="200"/>

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,250,0,0" Text="Log Type: " />
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,250,0,0" Text="{Binding LogType, Mode=TwoWay}" Width="200"/>

        <!-- Bouton Save -->
        <Button Content="Save" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20" Width="100"
                Command="{Binding SaveCommand}" />
    </Grid>
</UserControl>
