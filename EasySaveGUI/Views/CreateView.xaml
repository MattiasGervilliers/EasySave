<UserControl x:Class="EasySaveGUI.Views.CreateView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:EasySaveGUI.Views"
      xmlns:viewModels="clr-namespace:EasySaveGUI.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewModels:CreateViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="30" FontWeight="Bold" Text="Create a new Backup Configuration" />

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,100,0,0" Text="Name: " />
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,100,0,0" Text="{Binding Name, Mode=TwoWay}" Width="200"/>

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,150,0,0" Text="Log Path: " />
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,150,0,0" Text="{Binding SourcePath, Mode=TwoWay}" Width="200"/>

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,200,0,0" Text="State Path: " />
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,200,0,0" Text="{Binding DestinationPath, Mode=TwoWay}" Width="200"/>

        <!-- Bouton Save -->
        <Button Content="Create"
                HorizontalAlignment="Center" 
                VerticalAlignment="Bottom"
                Margin="0,0,0,20"
                Width="100"
                Command="{Binding CreateCommand}" />
    </Grid>
</UserControl>
